
import React from 'react';

/**
 * Per coding guidelines, the Gemini API key must be sourced exclusively from
 * the `process.env.API_KEY` environment variable. The application must not
 * provide any UI for entering or managing the API key.
 *
 * This component is provided to resolve compilation errors for an unused file.
 * It renders nothing and has no effect on the application.
 */
const ApiKeyManager: React.FC = () => {
  return null;
};

export default ApiKeyManager;
